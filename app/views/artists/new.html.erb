<html id="bg-search2">
  <% provide(:title) %>
  <div class="jumbotron.search">
    <!-- <h1>Search Artists</h1> -->
    <%= form_tag artists_path, :method => 'get' do %>
      artist:<%= text_field_tag :artist, params[:artist] %>
      <%= submit_tag "Search", :name => nil %><br />
      <%= link_to "View My Artists", myartists_path %>
    <% end %>
  </div>
</html>